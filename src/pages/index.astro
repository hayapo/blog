---
import Layout from "@/layouts/Layout.astro";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
import { css } from "root/styled-system/css";
import profileImage from "../assets/profile_square.webp";
import IconLink from "../components/IconLink.astro";
import { BLUESKY_ID, DISCORD_ID, MASTODON_ID, SITE_REPOSITORY_URL, TWITTER_ID, ZENN_ID } from "../consts.ts";

const defauleLayoutStyle = css({
  display: "flex",
  flexDirection: "column",
	alignItems: "center",
  flex: 1,
	textAlign: "center",
});

---

<Layout>
  <main class={defauleLayoutStyle}>
		<Image
		src={profileImage}
		alt="hayapoが寝転んでいる"
		width="250"
		height="250"
		class={css({
			borderRadius: '50%',
			mb: "2rem"
		})}/>
		<p class={css({fontFamily: "code",fontSize: "xx-large", fontWeight: "bold"})}>hayapo / Hayato Saito</p>
		<p class={css({fontSize: "large"})}>Web developer @Tokyo</p>
		<p class={css({fontSize: "large"})}>JavaScrip/TypeScript engineer</p>
		<div class={css({
			display: "flex",
			marginTop: "1rem",
			justifyContent: "center",
			alignItems: "center",
			gap: "0.5rem"
		})}>
			<IconLink href=`https://github.com/hayapo` title="github" name="mdi:github" color={css({color: "gray"})} />
			<IconLink href=`https://x.com/${TWITTER_ID}` title="twitter" name="mdi:twitter" color={css({color: "gray"})} />
			<IconLink href=`https://mstdn.jp/@${MASTODON_ID}` title="mastodon" name="mdi:mastodon" color={css({color: "gray"})} />
			<IconLink href=`https://bsky.app/profile/${BLUESKY_ID}` title="bluesky" name="ri:bluesky-fill" color={css({color: "gray"})} />
			<IconLink href=`https://discordapp.com/users/${DISCORD_ID}` title="discord" name="ri:discord-fill" color={css({color: "gray"})} />
			<IconLink href=`https://zenn.dev/${ZENN_ID}` title="zenn" name="simple-icons:zenn" color={css({color: "gray"})} isZenn/>
		</div>
  </main>
</Layout>
