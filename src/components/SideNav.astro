---
import { css } from "root/styled-system/css";
---

<aside
  class={css({
    top: "10rem",
    position: "sticky",
    display: { base: "none", sm: "flex" },
    flexDirection: "column",
  })}
>
  <div
    class={css({
      backgroundColor: "accent-background",
      color: "accent-background-text",
      gap: "2rem",
      p: "2rem",
      rounded: "0.7em",
    })}
  >
    <slot />
  </div>
</aside>
